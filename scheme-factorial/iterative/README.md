# Scheme Factorial: Iterative solution
A series of iterative solutions for factorials using Scheme. From `factorial-base` to `factorial-let` to `factorial-lambda`, each solution uses fewer macros than the last.
